//ASCII Value of an alphabet

#include <stdio.h>

int main()
{
    char alpha;
    printf ("Enter your alphabet: ");
    scanf ("%c", &alpha);
    
    printf ("The ASCII Value of your alphabet: %d", alpha);
}